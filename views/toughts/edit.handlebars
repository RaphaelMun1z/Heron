<div class="auth-form-container form-post">
    <h2>Editando publicação:</h2>
    <form action="/toughts/edit" method="POST">
        <input type="hidden" name="id" value="{{tought.id}}">
        <div class="form-control">
            <label for="title">O que gostaria de publicar?</label>
            <textarea name="title" cols="30" rows="10">{{tought.title}}</textarea>
        </div>
        <input type="submit" value="Salvar">
    </form>
    <a href="/toughts/dashboard" class="btn back">Voltar</a>
</div>